plugins {
    id 'java'
    id 'application'
    id 'com.gradleup.shadow' version '8.3.0'
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // Oracle JDBC 驱动，适合 Java 8+，可用于 Java 11
    implementation 'com.oracle.database.jdbc:ojdbc8:23.3.0.23.09'
}

application {
    mainClass = 'com.example.JdbcDemo'
}

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.example.JdbcDemo'
        )
    }
}

// 生成包含所有依赖的可执行 fat jar
shadowJar {
    archiveBaseName.set('jdbc-demo')
    archiveClassifier.set('')
    archiveVersion.set('')
    mergeServiceFiles()
}